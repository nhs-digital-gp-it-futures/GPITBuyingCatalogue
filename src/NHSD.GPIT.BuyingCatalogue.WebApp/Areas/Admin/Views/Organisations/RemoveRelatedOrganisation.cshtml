@using NHSD.GPIT.BuyingCatalogue.WebApp.Areas.Admin.Controllers
@using NHSD.GPIT.BuyingCatalogue.Framework.Extensions
@model NHSD.GPIT.BuyingCatalogue.WebApp.Areas.Admin.Models.OrganisationModels.RemoveRelatedOrganisationModel
@{
    ViewBag.Title = "Remove organisation?";
}

<div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-full">
        <partial name="Partials/_BackLink" model="Model" />

        <nhs-page-title title="@ViewBag.Title"
                        caption="@Model!.RelatedOrganisationName"
                        advice="@Model.OrganisationName will no longer be able to act on behalf of this organisation." />

        <form method="POST">
            <nhs-submit-button text="Remove organisation" />
        </form>

        <a asp-area="@typeof(OrganisationsController).AreaName()" 
           asp-controller="@typeof(OrganisationsController).ControllerName()" 
           asp-action="@nameof(OrganisationsController.RelatedOrganisations)" 
           asp-route-organisationId="@Model.OrganisationId">Cancel</a>
    </div>
</div>
