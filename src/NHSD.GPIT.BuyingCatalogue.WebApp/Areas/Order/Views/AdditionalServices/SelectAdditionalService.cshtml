@using NHSD.GPIT.BuyingCatalogue.EntityFramework.Catalogue.Models
@model NHSD.GPIT.BuyingCatalogue.WebApp.Areas.Order.Models.AdditionalServices.SelectAdditionalServiceModel;
@{
    ViewBag.Title = Model.Title;
}

<div data-test-id="additional-service-select-page">
    <partial name="Partials/_BackLink" model="Model" />
    <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-two-thirds">
            <nhs-validation-summary RadioId="SelectedAdditionalServiceId"/>
            <nhs-page-title title="@ViewBag.Title"
                            advice="These are the Additional Services for the Catalogue Solutions you've added to this order. Select the one you want to purchase from the list." />

            <form method="post">
                <input type="hidden" asp-for="@Model.BackLink" />
                <input type="hidden" asp-for="@Model.BackLinkText" />
                <input type="hidden" asp-for="@Model.OdsCode" />
                <input type="hidden" asp-for="@Model.Title" />
                <nhs-fieldset-form-label asp-for="@Model"
                                         label-text="Select Additional Service">
                    <nhs-radio-buttons asp-for="SelectedAdditionalServiceId"
                                       values="@Model.Solutions"
                                       value-name="@nameof(CatalogueItem.Id)"
                                       display-name="@nameof(CatalogueItem.Name)" />
                </nhs-fieldset-form-label>
                <nhs-submit-button text="Continue" />
            </form>
        </div>
    </div>
</div>
