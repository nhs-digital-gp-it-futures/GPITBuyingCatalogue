@using NHSD.GPIT.BuyingCatalogue.Framework.Extensions
@model NHSD.GPIT.BuyingCatalogue.WebApp.Models.OrganisationModels.RemoveNominatedOrganisationModel
@{
    ViewBag.Title = "Remove organisation?";
}

<div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-full">
        <partial name="Partials/_BackLink" model="Model" />

        <nhs-page-title title="@ViewBag.Title"
                        caption="@Model!.NominatedOrganisationName"
                        advice="This organisation will no longer be able to act on behalf of @Model.OrganisationName." />

        <form method="POST" action ="@Url.Action(
                                         nameof(OrganisationController.RemoveNominatedOrganisation),
                                         ViewContext.RouteData.Values["controller"].ToString())">
            <nhs-submit-button text="Remove organisation"/>
            @Html.AntiForgeryToken()
        </form>

        <a asp-controller=@ViewContext.RouteData.Values["controller"].ToString()
           asp-action="@nameof(OrganisationController.NominatedOrganisations)" 
           asp-route-organisationId="@Model.OrganisationId">Cancel</a>
    </div>
</div>
